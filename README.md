# Natura Labs Review

Persona post **Review Inbox** lives in [`review-app/`](review-app/).

```bash
cd review-app
cp .env.example .env.local
npm install
npm run dev
```

See [review-app/README.md](review-app/README.md) for Google OAuth (`@naturalabs.io`), n8n ingest (`POST /api/inbox`), approve webhooks, Firestore, and Cloud Run.
